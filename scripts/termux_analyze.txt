# Termux v3.5.1 — Signal + handshake peel + keylog (без pip cryptography)

# 1) Оновлення
curl -fL --retry 3 -o ~/analyze_pcap.py.new \
  "https://raw.githubusercontent.com/lizbeth307/lizbeth307/cursor/signal-pipeline-p4-a4e6/analyze_pcap.py?t=$(date +%s)"
mv ~/analyze_pcap.py.new ~/analyze_pcap.py

# 2) Pure AES-GCM helper (щоб --keylog працював без cryptography)
mkdir -p ~/protocol_ast
curl -fL -o ~/protocol_ast/__init__.py \
  "https://raw.githubusercontent.com/lizbeth307/lizbeth307/cursor/signal-pipeline-p4-a4e6/protocol_ast/__init__.py"
curl -fL -o ~/protocol_ast/aes_gcm.py \
  "https://raw.githubusercontent.com/lizbeth307/lizbeth307/cursor/signal-pipeline-p4-a4e6/protocol_ast/aes_gcm.py"
export PYTHONPATH=$HOME

python3 ~/analyze_pcap.py --version   # → 3.5.1-full

# 3) Signal (handshake peel — без ключів)
python3 ~/analyze_pcap.py ~/downloads/c.pcap --signal --flow TCP:443

# 4) Крізь opaque wall — потрібен SSLKEYLOGFILE з PCAPdroid/MITM
#    python3 ~/analyze_pcap.py ~/downloads/c.pcap --signal --flow TCP:443 \
#      --keylog ~/downloads/sslkeys.log
