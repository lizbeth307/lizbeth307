# === TERMUX: оновлення + глибокий аналіз ===

# 0. Python
pkg install python

# 1. Оновити (завантажує analyze_pcap.py + protocol_ast/deep_decode.py)
curl -fsSL "https://raw.githubusercontent.com/lizbeth307/lizbeth307/cursor/termux-blind-setup-a4e6/scripts/termux_setup.sh" | bash

# 2. Усі потоки (TLS SNI, QUIC varint, DNS, NTP, XMPP)
python3 ~/analyze_pcap.py ~/downloads/c.pcap --blind

# 3. Окремі потоки
python3 ~/analyze_pcap.py ~/downloads/c.pcap --blind --flow 443   # TLS + QUIC
python3 ~/analyze_pcap.py ~/downloads/c.pcap --blind --flow 53   # DNS domains
python3 ~/analyze_pcap.py ~/downloads/c.pcap --blind --flow 5222 # Google XMPP/mtalk
python3 ~/analyze_pcap.py ~/downloads/c.pcap --blind --flow 123   # NTP

# Звіт: ~/downloads/blind_report.json
