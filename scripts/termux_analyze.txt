# === TERMUX: скопіюйте по черзі ===

# 0. Python (ОБОВ'ЯЗКОВО — без цього python3 не працює)
pkg install python

# 1. Автоматичне налаштування (одна команда):
curl -fsSL "https://raw.githubusercontent.com/lizbeth307/lizbeth307/cursor/termux-blind-setup-a4e6/scripts/termux_setup.sh" | bash

# 2. Аналіз вашого PCAP (ім'я файлу на ТЕЛЕФОНІ, не phone_capture.pcap з хмари):
python3 ~/analyze_pcap.py ~/downloads/PCAPdroid_25_лип._12_42_30.pcap

# 3. Сліпий аналіз TLS/QUIC (аналог discover_protocol_ast.py blind):
python3 ~/analyze_pcap.py ~/downloads/PCAPdroid_25_лип._12_42_30.pcap --blind --flow 443

# АБО через обгортку (обхід grep /proc/stat):
bash ~/run_pcap.sh ~/downloads/PCAPdroid_25_лип._12_42_30.pcap

# Звіт: ~/downloads/probe_report.json або blind_report.json

# --- Повний discover_protocol_ast.py (потрібен git + клон репо) ---
# pkg install git python
# git clone https://github.com/lizbeth307/lizbeth307.git
# cd lizbeth307 && git checkout cursor/protocol-ast-proto-a4e6
# python3 discover_protocol_ast.py blind ~/downloads/PCAPdroid*.pcap --flow 443
